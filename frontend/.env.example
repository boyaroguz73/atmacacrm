# Docker build / runtime — kök dizindeki .env ile aynı satırları kullanın (compose ARG/ENV).
# VPS örnek: http://SUNUCU_IP:4000/api ve http://SUNUCU_IP:4000
# Tek domain + HTTPS: https://crm.ornek.com/api ve https://crm.ornek.com

NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_WS_URL=http://localhost:4000
